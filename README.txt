Install terraform on the PC you plan to run the script from.
To run the code use either Visul Studio Code or Atom and open the files from the location of the folder, where the terraform folder is and run the following commands:
terraform init
terraform plan
terraform apply
After the work on the environment is done and you no longer need the application, ypou can destroy the environment by the below command:
terraform destroy